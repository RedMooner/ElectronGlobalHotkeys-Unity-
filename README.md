# ElectronGlobalHotkeys-Unity-
this software is needed to create global hotkeys in the game on the unity engine
# JS using:
    add("hotkey", callback);
# example:
    add("Ctrl+G", ChangeGameMode);
    
# Unity using
